overwrite: true
schema: ./exports/graphql/schema.graphql
# documents: "src/**/*.graphql"
generates:
  graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-react-apollo"
  ./graphql.schema.json:
    plugins:
      - "introspection"
